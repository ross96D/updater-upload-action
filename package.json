{
  "name": "updater-upload-action",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle --format=cjs --platform=node src/index.js --outfile=dist/index.js",
    "test": "npx vitest run --reporter=basic --testTimeout=12000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  }
}
